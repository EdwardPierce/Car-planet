import LoadingSpinner from "@/layers/lib/LoadingSpinner";
import React from "react";

const Loading = () => {
  return (
    <div>
      <LoadingSpinner />
    </div>
  );
};

export default Loading;
